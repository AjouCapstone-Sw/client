export type AdminPageInfo = {
  commission: number;
  totalPrice: number;
};

export type SellProduct = {
  buyer: string;
  commission: number;
  image: string;
  price: number;
  seller: string;
  title: string;
};

import styled from 'styled-components';

const Container = styled.div`
  display: flex;

  input {
    margin-right: 20px;
  }
  button {
    width: 40%;
    min-width: 120px;
  }
`;

export default { Container };

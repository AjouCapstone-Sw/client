import { Snackbar as MSnackBar } from '@mui/material';
import styled from 'styled-components';

export const Snackbar = styled(MSnackBar)`
  position: fixed;
  align-items: flex-start !important;
  top: 24px !important;
  left: 24px !important;
  bottom: 0px !important;
`;
